# 复制本文件为 .env.development.local / .env.production.local 后按需填写。
# 仅 VITE_ 前缀变量会注入前端代码，见：https://cn.vitejs.dev/guide/env-and-mode.html

# 支付跳转、生产环境 axios 直连时使用的 API 源站（末尾不要斜杠）
# 未设置时使用 src/utils/apiBase.ts 中的默认测试域
# VITE_API_ORIGIN=https://your-api.example.com

# 可选：强制覆盖 axios baseURL（一般留空；开发环境默认 /api 走 vite 代理）
# VITE_API_BASE=
